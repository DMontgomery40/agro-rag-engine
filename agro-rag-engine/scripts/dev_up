#!/usr/bin/env bash
set -euo pipefail

# Thin wrapper so Quick Start can use: cd agro/scripts && ./dev_up
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$SCRIPT_DIR/dev_up.sh" "$@"

