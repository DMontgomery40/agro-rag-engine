// Fetch URL rewriter - no rewriting needed, API uses /api/* directly
(() => {
  'use strict';
  // No rewriting needed - API is at /api/* not /api/*
  console.log('[fetch-shim] Loaded (no rewriting)');
})();
