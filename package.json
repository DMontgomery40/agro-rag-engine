{
  "name": "agro-e2e",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "test:e2e": "playwright test",
    "test:ui": "playwright test --ui",
    "test:gui": "playwright test --config=playwright.gui.config.ts",
    "test:gui:headed": "playwright test --config=playwright.gui.config.ts --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2"
  }
}

