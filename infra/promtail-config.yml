server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # Scrape Docker container logs
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      # Only scrape logs from containers with specific labels
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
      - source_labels: ['__meta_docker_container_log_stream']
        target_label: 'stream'
      - source_labels: ['__meta_docker_container_label_com_docker_compose_project']
        target_label: 'compose_project'
      - source_labels: ['__meta_docker_container_label_com_docker_compose_service']
        target_label: 'compose_service'
    pipeline_stages:
      # Parse JSON logs if present
      - json:
          expressions:
            level: level
            msg: msg
            timestamp: timestamp
      # Extract log level from message if not in JSON
      - regex:
          expression: '^(?P<time>\S+) (?P<level>\S+) (?P<message>.*)$'
      - labels:
          level:
          stream:

  # Scrape system logs
  - job_name: system
    static_configs:
      - targets:
          - localhost
        labels:
          job: varlogs
          __path__: /var/log/*.log

  # Scrape AGRO tracking JSONL files produced by the API container
  - job_name: agro-tracking-jsonl
    static_configs:
      - targets:
          - localhost
        labels:
          job: agro-tracking
          __path__: /app/data/tracking/*.jsonl
    pipeline_stages:
      - json:
          expressions:
            ts: ts
            type: type
            repo: repo
      - labels:
          type:
          repo:
