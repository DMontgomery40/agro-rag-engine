name: Publish MkDocs (mike)

on:
  push:
    branches: [development, staging, main]
    paths:
      - 'mkdocs.yml'
      - 'mkdocs/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install mkdocs + material + plugins + mike
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs mkdocs-material mkdocs-glightbox mike

      - name: Build (sanity check)
        run: |
          mkdocs build --strict

      - name: Configure git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Deploy latest to gh-pages
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mike deploy --push --remote origin --branch gh-pages latest -m "docs: publish latest from main"
          mike set-default --push --remote origin --branch gh-pages latest
